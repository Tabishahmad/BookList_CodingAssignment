<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.BookListCompose" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/purple_700</item>
    </style>
    <!-- Splash Screen Theme (API >= 31) -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- The splash screen background, animated icon, and animation duration -->
        <item name="windowSplashScreenBackground">@color/purple_200</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/animated_face</item>
        <item name="windowSplashScreenAnimationDuration">2000</item>
        <item name="postSplashScreenTheme">@style/Theme.BookListCompose</item>
    </style>
</resources>